// in a for loop, inside the parenthrsis, there atr trhree parts:
// let i = 5--> where to start our loop from
// middle part ( i>=1) --> when to stop looping the process
// third part ( i--) --> what to do with our variable after each iteration 
let nbaGoats = ["Lebron", "Kobe", "Jordan", "Shaq"];
function printArr(arr) {
    for (let i = 0; i <= 4; i++) {
        console.log(nbaGoats[i])
    }
}
printArr(nbaGoats)

let Nums = [15, 22, 33, 77, 99, 88, 55];
function PrintgreaterthanValue(arr) {
    let secondVal = arr[i]
    for (let i = 0; i < arr.length; i++) {
        console.log(arr(i));
    }
}